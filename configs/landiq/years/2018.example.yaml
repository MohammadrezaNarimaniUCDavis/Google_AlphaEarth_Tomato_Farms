# Paste under landiq: in configs/paths.local.yaml (merge with your paths)

year: 2018
# If the .shp is only under derived extract, not raw:
# input_shapefile: "data/derived/landiq_extracted/2018/<folder>/<stem>.shp"
zip_filename: null  # set to exact zip in data/raw/landiq/2018/ or rely on auto-detect
tomato_values: ["T15", "T26"]
crop_columns: ["CROPTYP1", "CROPTYP2", "CROPTYP3"]
