# Shared LandIQ notebooks (all survey years)

| Notebook | Purpose |
|----------|---------|
| [`explore_landiq_year.ipynb`](explore_landiq_year.ipynb) | One notebook for **2018–2024**: set `YEAR`, unzip, scan attributes and `CROPTYP*` for tomato codes. |

Year-specific tutorials (detailed **2018** walkthrough) live in **[`../2018/`](../2018/)**. Numeric folders under **`years/`** are only survey years — the generic driver stays **here** so the layout stays clear.
