# Shared LandIQ notebooks (all survey years)

| Notebook | Purpose |
|----------|---------|
| [`explore_landiq_year.ipynb`](explore_landiq_year.ipynb) | One notebook for **2016, 2018–2024**: set `YEAR`, unzip, scan attributes and `CROPTYP*` for tomato codes. |

Year-specific tutorials (e.g. 2016 detail) live in **[`../2016/`](../2016/)**. Numeric folders under **`years/`** are only survey years — the generic driver stays **here** so the layout stays clear.
