# Pixelwise tomato modeling (SageMaker GPU / Linux or pip default CUDA wheel).
# For Windows CPU dev, see guide/03-data-s3-and-training.md (CPU torch first, then requirements-modeling-cpu.txt).
#
#   pip install -r requirements-modeling.txt
#   pip install -e .

torch>=2.2,<2.3
rasterio>=1.3
pandas>=2.0
numpy>=1.24
pyyaml>=6
tqdm>=4.66
pyarrow>=14.0
