# Geospatial stack (local + SageMaker base compatibility)
geopandas>=0.14.0
pandas>=2.0.0
shapely>=2.0.0
pyproj>=3.6.0
fiona>=1.9.0
rasterio>=1.3.0
pyyaml>=6.0

# Notebooks
jupyter>=1.0.0
ipykernel>=6.0.0
pypdf>=4.0.0

# Optional: Google Earth Engine Python API (uncomment if you use EE for Alpha Earth)
# earthengine-api>=0.1.375

# Add for deep learning on SageMaker or local GPU, e.g.:
# torch>=2.0.0
# torchvision>=0.15.0
