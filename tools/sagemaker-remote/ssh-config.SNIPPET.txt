# Snippet for ~/.ssh/config (Windows: C:\Users\<you>\.ssh\config)
#
# Do NOT copy blindly. Hostnames for SageMaker Studio remote IDE are specific
# to your space and are usually provided by AWS (Console deep link or Toolkit).
# For a normal EC2 GPU box you would use something like:
#
# Host ec2-gpu-alphaearth
#     HostName <public-dns-or-ip>
#     User ec2-user
#     IdentityFile ~/.ssh/your-key.pem
#
# SageMaker Studio Remote IDE typically uses ProxyCommand with AWS SSM —
# follow AWS docs and let the AWS Toolkit generate or document the block.
